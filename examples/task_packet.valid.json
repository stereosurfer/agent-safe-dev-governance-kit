{
  "mode": "github_unavailable_fallback",
  "github_issue_status": "pending_unavailable",
  "lane": "validation_tooling",
  "intelligence_level": "advanced",
  "reason": "The fixture exercises the dependency-free fallback validator.",
  "durable_source_of_truth": "Repository task packet pending GitHub recovery.",
  "objective": "Validate one complete JSON fallback packet.",
  "plan": [
    "Run task-packet-check."
  ],
  "checklist": [
    "Validator passes."
  ],
  "acceptance_sheet": [
    "The fallback is structurally complete."
  ],
  "allowed_paths": [
    "scripts/asgk.py",
    "examples/task_packet.valid.json"
  ],
  "context_read_set": [
    "AGENTS.md",
    "examples/task_packet.valid.json"
  ],
  "project_specific_validation": [
    "python3 scripts/asgk.py task-packet-check --file examples/task_packet.valid.json"
  ],
  "expected_output": "Task packet check passes with only outage-bounded local scope.",
  "non_goals": [
    "No external action, protected-path exception, PR, or merge authority."
  ],
  "stop_conditions": [
    "GitHub issue is still unavailable before PR creation."
  ],
  "rollback_expectations": "Remove the fixture and its focused validator change."
}
