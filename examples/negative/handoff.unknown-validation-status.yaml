negative_case:
  id: NEG-034-handoff-unknown-validation-status
  title: Handoff packet has unknown validation status
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#000"
  active_pr: "#000"
  branch: "codex/example"
  objective: "Bad example with unknown validation status."
  current_state: "Bad example."
  completed:
    - "Created bad fixture."
  remaining:
    - "Should be blocked."
  allowed_paths:
    - "docs/"
  modified_files:
    - "docs/example.md"
  validation_status:
    status: "unknown"
    reason: "negative fixture"
  blockers: "none"
  next_safe_action: "Block this handoff packet."
  must_read:
    - "AGENTS.md"
  must_not_do:
    - "Do not proceed."
  decisions:
    - "This is a negative fixture."
  open_questions: "none"
