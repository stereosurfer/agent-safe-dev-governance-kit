negative_case:
  id: NEG-034-handoff-unknown-validation-status
  title: Handoff packet has unsupported validation status
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#333"
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise the closed validation-status enum."
  current_state: "All other core fields are materially populated."
  remaining:
    - "Block this packet."
  allowed_paths:
    - "examples/negative/handoff.unknown-validation-status.yaml"
  modified_files:
    - "examples/negative/handoff.unknown-validation-status.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue from this packet."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "unknown"
    evidence:
      - "Negative fixture input."
    reason: "Unknown is intentionally unsupported."
  blockers:
    - "validation status is invalid"
  next_safe_action: "Reject this packet with HP_VALIDATION_STATUS_INVALID."
