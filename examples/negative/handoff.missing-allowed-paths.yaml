negative_case:
  id: NEG-035-handoff-missing-allowed-paths
  title: Handoff packet missing allowed_paths
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#333"
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise a missing write boundary."
  current_state: "All other core fields are materially populated."
  remaining:
    - "Block this packet."
  modified_files:
    - "examples/negative/handoff.missing-allowed-paths.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue without allowed paths."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "not_run"
    evidence:
      - "Negative fixture input."
    reason: "The checker has not run yet."
  blockers:
    - "allowed_paths is missing"
  next_safe_action: "Reject this packet with HP_FIELD_MISSING."
