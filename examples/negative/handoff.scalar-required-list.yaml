negative_case:
  id: NEG-W3B-002-handoff-scalar-required-list
  title: Handoff packet uses a scalar where allowed_paths must be a list
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#333"
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise nested type validation."
  current_state: "All fields except allowed_paths use the canonical type."
  remaining:
    - "Reject this packet."
  allowed_paths: "examples/negative/handoff.scalar-required-list.yaml"
  modified_files:
    - "examples/negative/handoff.scalar-required-list.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue from this packet."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "not_run"
    evidence:
      - "Negative fixture input."
    reason: "The checker has not run yet."
  blockers:
    - "allowed_paths has the wrong type"
  next_safe_action: "Reject this packet with HP_FIELD_TYPE_INVALID."
