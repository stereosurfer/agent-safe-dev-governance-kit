negative_case:
  id: NEG-032-handoff-missing-active-issue
  title: Handoff packet missing active_issue
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise a missing active issue."
  current_state: "All other core fields are materially populated."
  remaining:
    - "Block this packet."
  allowed_paths:
    - "examples/negative/handoff.missing-active-issue.yaml"
  modified_files:
    - "examples/negative/handoff.missing-active-issue.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue from this packet."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "not_run"
    evidence:
      - "Negative fixture input."
    reason: "The checker has not run yet."
  blockers:
    - "active_issue is missing"
  next_safe_action: "Reject this packet with HP_FIELD_MISSING."
