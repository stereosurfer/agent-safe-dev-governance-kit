negative_case:
  id: NEG-076-task-packet-legacy-reason-alias
  expected_codes:
    - TP_LEGACY_FIELD_FORBIDDEN
    - TP_REQUIRED_FIELD_MISSING
  should_block_positive_validation: false

bad_input:
  mode: "github_unavailable_fallback"
  github_issue_status: "pending_unavailable"
  lane: "validation_tooling"
  intelligence_level: "standard"
  intelligence_level_reason: "Legacy alias must not satisfy reason."
  durable_source_of_truth: "Repository task packet pending GitHub recovery."
  objective: "Reject the v1 reason alias."
  plan:
    - "Run the negative check."
  checklist:
    - "The validator rejects this packet."
  acceptance_sheet:
    - "Legacy and missing-reason findings are reported."
  allowed_paths:
    - "examples/task_packet.example.yaml"
  context_read_set:
    - "AGENTS.md"
  project_specific_validation:
    - "python3 scripts/asgk.py task-packet-check"
  expected_output: "Focused alias findings."
  non_goals:
    - "No v1 compatibility alias."
  stop_conditions:
    - "The alias is accepted."
  rollback_expectations: "Remove this negative fixture."
