# Target Install Negative Fixtures

These fixtures are intentionally invalid target repositories for the opt-in
target-install negative command.

Run them with:

```bash
python3 scripts/asgk.py negative target-install
```

They are not wired into default CI.
