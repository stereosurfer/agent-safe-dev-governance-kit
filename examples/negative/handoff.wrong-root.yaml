negative_case:
  id: NEG-W3B-001-handoff-wrong-root
  title: Handoff content is nested under a non-canonical root
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_bundle:
  active_issue: "#333"
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise the canonical root requirement."
  current_state: "The content is intentionally under the wrong root."
  remaining:
    - "Reject this packet."
  allowed_paths:
    - "examples/negative/handoff.wrong-root.yaml"
  modified_files:
    - "examples/negative/handoff.wrong-root.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue from this packet."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "not_run"
    evidence:
      - "Negative fixture input."
    reason: "The checker has not run yet."
  blockers:
    - "canonical root is missing"
  next_safe_action: "Reject this packet with HP_PACKET_ROOT_MISSING."
