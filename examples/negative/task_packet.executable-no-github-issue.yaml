bad_input:
  task_id: "NEG-030"
  lane: "lane_06_ci_github"
  intelligence_level: "advanced"
  intelligence_level_reason: "Validation tooling changes require careful governance judgment."
  durable_source_of_truth: "docs/control/TASK_PACKET_FORMAT.md"
  github_issue_status: "available"
  work_unit_kind: "implementation"
  objective: "Change validator behavior without a GitHub issue."
  product_context: "This should be blocked because task packets cannot replace issue-first authorization."
  current_repository_context: "Task packet validator exists."
  files_to_inspect_first:
    - "AGENTS.md"
    - "docs/control/TASK_PACKET_FORMAT.md"
  allowed_paths:
    - "scripts/asgk.py"
  expected_changes:
    - "Modify validator behavior."
  expected_output: "This fixture must fail task-packet-check."
  non_goals:
    - "No release execution."
  constraints:
    - "GitHub is available."
  plan:
    - "Attempt to work from a task packet only."
  checklist:
    - "[ ] Validator blocks this packet."
  acceptance_sheet:
    - "[ ] task-packet-check fails."
  validation_commands:
    - "python3 scripts/asgk.py task-packet-check --file examples/negative/task_packet.executable-no-github-issue.yaml"
  stop_conditions:
    - "missing_github_issue"
  rollback_expectations: "Remove this negative fixture."
