negative_case:
  id: NEG-001-see-chat-source
  title: Task packet uses chat as durable source of truth
  expected_outcome: blocked
  owner: task_packet_validator_or_pr_review
  should_block_positive_validation: false

bad_input:
  task_id: "NEG-001"
  lane: "lane_00_controller"
  intelligence_level: "standard"
  intelligence_level_reason: "bad example"
  durable_source_of_truth: "see chat"
  objective: "This should be blocked because chat is not durable authority."
  allowed_paths:
    - "docs/"
  expected_output: "blocked task packet"
  plan:
    - "Do work from chat-only scope."
  checklist:
    - "[ ] invalid"
  acceptance_sheet:
    - "[ ] invalid"
  stop_conditions:
    - "missing_durable_source_of_truth"
  rollback_expectations: "Close without work."
