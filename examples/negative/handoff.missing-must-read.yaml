negative_case:
  id: NEG-036-handoff-missing-must-read
  title: Handoff packet missing must_read
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#000"
  active_pr: "#000"
  branch: "codex/example"
  objective: "Bad example missing must_read."
  current_state: "Bad example."
  completed:
    - "Created bad fixture."
  remaining:
    - "Should be blocked."
  allowed_paths:
    - "docs/"
  modified_files:
    - "docs/example.md"
  validation_status:
    status: "not_run"
    reason: "negative fixture"
  blockers: "none"
  next_safe_action: "Block this handoff packet."
  must_not_do:
    - "Do not proceed."
  decisions:
    - "This is a negative fixture."
  open_questions: "none"
