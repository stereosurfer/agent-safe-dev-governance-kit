negative_case:
  id: NEG-036-handoff-missing-must-read
  title: Handoff packet missing must_read
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#333"
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise a missing recovery read set."
  current_state: "All other core fields are materially populated."
  remaining:
    - "Block this packet."
  allowed_paths:
    - "examples/negative/handoff.missing-must-read.yaml"
  modified_files:
    - "examples/negative/handoff.missing-must-read.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue without the read set."
  validation_status:
    status: "not_run"
    evidence:
      - "Negative fixture input."
    reason: "The checker has not run yet."
  blockers:
    - "must_read is missing"
  next_safe_action: "Reject this packet with HP_FIELD_MISSING."
