negative_case:
  id: NEG-038-handoff-unresolved-todo
  title: Handoff packet retains a TODO during strict final checking
  expected_outcome: blocked
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#333"
  active_pr: "none; negative fixture"
  durable_source_of_truth:
    - "GitHub issue #333"
  branch: "codex/example"
  objective: "Exercise strict TODO rejection."
  current_state: "AI_TODO: replace this before final handoff."
  remaining:
    - "Block this packet."
  allowed_paths:
    - "examples/negative/handoff.unresolved-todo.yaml"
  modified_files:
    - "examples/negative/handoff.unresolved-todo.yaml"
  non_goals:
    - "Do not infer approval."
  must_not_do:
    - "Do not continue from a draft packet."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "not_run"
    evidence:
      - "Negative fixture input."
    reason: "The checker has not run yet."
  blockers:
    - "unresolved TODO remains"
  next_safe_action: "Reject this packet with HP_TODO_UNRESOLVED."
