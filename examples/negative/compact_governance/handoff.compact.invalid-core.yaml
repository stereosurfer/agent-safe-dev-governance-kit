compact_handoff:
  active_issue: "#333"
  active_pr: "#334"
  durable_source_of_truth:
    - "GitHub issue #333"
    - "GitHub PR #334"
  branch: "codex/example"
  objective: "Prove compact validation returns core findings before freshness."
  current_state: "allowed_paths intentionally has the wrong type."
  remaining:
    - "Reject this packet before reading CURRENT_STATUS."
  allowed_paths: "examples/negative/compact_governance/handoff.compact.invalid-core.yaml"
  modified_files:
    - "examples/negative/compact_governance/handoff.compact.invalid-core.yaml"
  non_goals:
    - "Do not run freshness checks."
  must_not_do:
    - "Do not suppress the HP finding."
  must_read:
    - "AGENTS.md"
  validation_status:
    status: "fail"
    evidence:
      - "Negative fixture input."
    reason: "The core packet is intentionally invalid."
  blockers:
    - "allowed_paths has the wrong type"
  next_safe_action: "Return HP_FIELD_TYPE_INVALID with freshness_checked false."
  current_status_impact:
    status: "not_applicable"
    reason: "Freshness must not run after a core failure."
    current_status_updated_in_this_pr: false
    post_merge_safe: "not_applicable"
    follow_up_issue: "none"
