positive_case:
  id: POS-001-handoff-template-valid-packet
  title: Filled handoff-template output passes handoff-check
  expected_outcome: passes
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#56"
  active_pr: "none; fixture describes template/checker alignment before PR creation"
  branch: "codex/align-handoff-template-check"
  objective: "Prove that filled handoff-template-style list output passes handoff-check."
  current_state: "Issue #56 authorizes a bounded validator and fixture alignment work unit."
  completed:
    - "Created a concrete positive handoff fixture using list-shaped fields."
  remaining:
    - "Run handoff-check, doctor, and GitHub Actions before merge."
  allowed_paths:
    - "scripts/asgk.py"
    - "examples/handoff_packet.valid.yaml"
    - ".github/workflows/bootstrap-validation.yml"
    - "docs/control/HANDOFF_PACKET.md"
  modified_files:
    - "scripts/asgk.py"
    - "examples/handoff_packet.valid.yaml"
  validation_status:
    status: "not_run"
    evidence: "Validation is expected to be run by local command or CI after this fixture is updated."
  blockers: "none"
  next_safe_action: "Run python3 scripts/asgk.py handoff-check --file examples/handoff_packet.valid.yaml --fail-on-todo."
  must_read:
    - "AGENTS.md"
    - "scripts/asgk.py"
    - "examples/handoff_packet.valid.yaml"
    - "GitHub issue #56"
  must_not_do:
    - "Do not add dependencies or introduce a full YAML parser."
    - "Do not expand into closeout policy, uncontrolled-document audit, or field-test work."
  decisions:
    - "The fixture intentionally uses list-shaped allowed_paths and must_read fields to match handoff-template output."
  open_questions:
    - "None for this fixture."
