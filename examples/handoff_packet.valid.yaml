positive_case:
  id: POS-W3B-001-typed-handoff-core
  title: Filled canonical handoff packet passes the typed core evaluator
  expected_outcome: passes
  owner: asgk_handoff_check
  should_block_positive_validation: false

handoff_packet:
  active_issue: "#333 ASGK 2.0 W3B"
  active_pr: "none; fixture models the state before PR creation"
  durable_source_of_truth:
    - "GitHub issue #333"
    - "GitHub issue #323 owner-approved program comment"
  branch: "codex/asgk-2-w3b-handoff-status-convergence"
  objective: "Prove the canonical typed handoff core and fixture agree."
  current_state: "Issue authority passed and the bounded implementation is in progress."
  remaining:
    - "Run the W3B validation set and open the protected PR."
  allowed_paths:
    - "scripts/asgk.py"
    - "scripts/asgk_lib/handoff.py"
    - "examples/handoff_packet.valid.yaml"
  modified_files:
    - "scripts/asgk.py"
    - "scripts/asgk_lib/handoff.py"
    - "examples/handoff_packet.valid.yaml"
  non_goals:
    - "Do not implement W3C's shared validation envelope."
  must_not_do:
    - "Do not modify paths outside issue #333."
    - "Do not infer approval from this fixture passing."
  must_read:
    - "AGENTS.md"
    - "docs/handoff/CURRENT_STATUS.md"
    - "GitHub issue #333"
  validation_status:
    status: "not_run"
    evidence:
      - "This fixture is the input to the validation command."
    reason: "The fixture describes the pre-validation state."
  blockers:
    - "none; no known blocker in this fixture"
  next_safe_action: "Run handoff-check with strict draft-marker checking and JSON output."
